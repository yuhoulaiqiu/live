syntax = "v1"

type LogListRequest {}

type LogListResponse {}

service log {
	@handler logList
	get /api/log/log (LogListRequest) returns (LogListResponse)
}

// goctl api go -api log_api.api -dir . --home ../../template
